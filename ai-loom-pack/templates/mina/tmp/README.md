# Disposable Scratch

Use task-specific subdirectories for non-secret temporary material. Keep manual
acceptance fixtures under a `.loom-acceptance` subdirectory. Scratch is not
canonical state, memory, a credential store, or recovery evidence. Clean up only
the current task's explicitly disposable artifacts within its authority.
