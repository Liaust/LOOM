# LOOM backport for pinned ORCA 1.4.191. Behavior reference:
# https://github.com/stablyai/orca/pull/13359 (d3b2b6b8a3eecd3d53a8426034388368d445f73f)
# Pinned flags.js has no rejectValuelessFlag: validate raw Map values here.
--- a/resources/app.asar.unpacked/out/cli/bundled-skill-guides.js
+++ b/resources/app.asar.unpacked/out/cli/bundled-skill-guides.js
@@ -1,4 +1,11 @@
 "use strict";
+// Keep the generated guide unchanged except for the version-matched folder example.
+function folderBootstrapGuide(markdown) {
+    return markdown.replace(
+        "ORCA repo add --path /abs/repo --json\n",
+        "ORCA repo add --path /abs/repo --json\nORCA repo add --path /abs/notes --kind folder --json\n"
+    );
+}
 // Generated by config/scripts/generate-bundled-skill-guides.mjs. Do not edit.
 Object.defineProperty(exports, "__esModule", { value: true });
 exports.BUNDLED_SKILL_GUIDES = void 0;
@@ -38,8 +45,8 @@
     {
         name: "orca-cli",
         description: "Use the public `orca` CLI to operate Orca-managed worktrees, folder contexts, terminals, repos, automations, artifacts, skill sharing, worktree comments, and the browser embedded inside the Orca app. Use when the user says \"$orca-cli\", \"use orca cli\", \"Orca worktree\", \"child worktree\", \"cardStatus\", \"spawn codex/claude in a worktree\", \"read/wait/send Orca terminal\", \"terminal send\", \"full handoff\", \"handover\", \"give this to another agent\", \"another worktree\", \"Orca browser\", \"orca artifacts\", \"share HTML/Markdown\", \"public artifact link\", \"share skills\", or \"control the browser inside Orca\". Prefer this over raw `git worktree`, ad hoc PTYs, Playwright, or Computer Use when the task touches Orca-managed state. Use Computer Use for browser windows, webviews, or desktop UI outside Orca's embedded browser.",
-        markdown: ORCA_CLI_MARKDOWN,
-        fullMarkdown: ORCA_CLI_MARKDOWN,
+        markdown: folderBootstrapGuide(ORCA_CLI_MARKDOWN),
+        fullMarkdown: folderBootstrapGuide(ORCA_CLI_MARKDOWN),
         aliases: []
     },
     {
--- a/resources/app.asar.unpacked/out/cli/handlers/project.js
+++ b/resources/app.asar.unpacked/out/cli/handlers/project.js
@@ -5,6 +5,7 @@
 const execution_host_flag_1 = require("../execution-host-flag");
 const flags_1 = require("../flags");
 const repo_path_arguments_1 = require("../repo-path-arguments");
+const repo_kind_flag_1 = require("../repo-kind-flag");
 const runtime_client_1 = require("../runtime-client");
 // Why: an Orca server that predates project host setup answers `method_not_found`, which reads
 // as an Orca bug rather than a version gap — and since --host runtime:<id> now routes these
@@ -42,16 +43,6 @@
     }
     return host.id;
 }
-function getOptionalRepoKind(flags) {
-    const kind = (0, flags_1.getOptionalStringFlag)(flags, 'kind');
-    if (kind === undefined) {
-        return undefined;
-    }
-    if (kind === 'git' || kind === 'folder') {
-        return kind;
-    }
-    throw new runtime_client_1.RuntimeClientError('invalid_argument', '--kind must be git or folder');
-}
 exports.PROJECT_HANDLERS = {
     'project list': async ({ client, json }) => {
         const result = await client.call('project.list');
@@ -71,7 +62,7 @@
             projectId: (0, flags_1.getRequiredStringFlag)(flags, 'project'),
             hostId: getRequiredHostId(flags),
             path: (0, repo_path_arguments_1.resolveRepoPathArgument)(rawPath, cwd, client.isRemote, 'Remote project setup'),
-            kind: getOptionalRepoKind(flags),
+            kind: (0, repo_kind_flag_1.getOptionalRepoKind)(flags),
             displayName: (0, flags_1.getOptionalStringFlag)(flags, 'display-name')
         };
         const result = await callProjectHostSetup(client, 'projectHostSetup.setupExistingFolder', args);
@@ -90,6 +81,7 @@
         (0, format_1.printResult)(result, json, format_1.formatProjectHostSetupResult);
     },
     'project setup-create': async ({ flags, client, cwd, json }) => {
+        const kind = (0, repo_kind_flag_1.getOptionalRepoKind)(flags);
         const path = (0, flags_1.getOptionalStringFlag)(flags, 'path');
         const args = {
             projectId: (0, flags_1.getRequiredStringFlag)(flags, 'project'),
@@ -103,7 +95,7 @@
             path: path === undefined
                 ? undefined
                 : (0, repo_path_arguments_1.resolveRepoPathArgument)(path, cwd, client.isRemote, 'Project setup create'),
-            kind: getOptionalRepoKind(flags),
+            kind,
             displayName: (0, flags_1.getOptionalStringFlag)(flags, 'display-name'),
             worktreeBasePath: (0, flags_1.getOptionalStringFlag)(flags, 'worktree-base-path'),
             gitUsername: (0, flags_1.getOptionalStringFlag)(flags, 'git-username'),
@@ -124,7 +116,7 @@
                     : (0, repo_path_arguments_1.resolveRepoPathArgument)(path, cwd, client.isRemote, 'Project setup update'),
                 worktreeBasePath: (0, flags_1.getOptionalStringFlag)(flags, 'worktree-base-path'),
                 gitUsername: (0, flags_1.getOptionalStringFlag)(flags, 'git-username'),
-                kind: getOptionalRepoKind(flags),
+                kind: (0, repo_kind_flag_1.getOptionalRepoKind)(flags),
                 setupState: getOptionalSetupState(flags),
                 setupMethod: getOptionalSetupMethod(flags)
             }
--- a/resources/app.asar.unpacked/out/cli/handlers/repo.js
+++ b/resources/app.asar.unpacked/out/cli/handlers/repo.js
@@ -4,6 +4,7 @@
 const format_1 = require("../format");
 const flags_1 = require("../flags");
 const repo_path_arguments_1 = require("../repo-path-arguments");
+const repo_kind_flag_1 = require("../repo-kind-flag");
 exports.REPO_HANDLERS = {
     'repo list': async ({ client, json }) => {
         const result = await client.call('repo.list');
@@ -11,8 +12,10 @@
     },
     'repo add': async ({ flags, client, cwd, json }) => {
         const repoPath = (0, flags_1.getRequiredStringFlag)(flags, 'path');
+        const kind = (0, repo_kind_flag_1.getOptionalRepoKind)(flags);
         const result = await client.call('repo.add', {
-            path: (0, repo_path_arguments_1.resolveRepoPathArgument)(repoPath, cwd, client.isRemote, 'Remote repo add')
+            path: (0, repo_path_arguments_1.resolveRepoPathArgument)(repoPath, cwd, client.isRemote, 'Remote repo add'),
+            ...(kind === undefined ? {} : { kind })
         });
         (0, format_1.printResult)(result, json, format_1.formatRepoShow);
     },
--- a/resources/app.asar.unpacked/out/cli/help.js
+++ b/resources/app.asar.unpacked/out/cli/help.js
@@ -264,7 +264,7 @@
   orca project setup-update --setup <setup-id> [--display-name <name>] [--path <path>] [--worktree-base-path <path>] [--git-username <name>] [--kind git|folder] [--state ready|not-set-up|setting-up|error|unsupported] [--method legacy-repo|imported-existing-folder|cloned|provisioned] [--json]
   orca project setup-delete --setup <setup-id> [--json]
   orca repo list [--json]
-  orca repo add --path <path> [--json]
+  orca repo add --path <path> [--kind git|folder] [--json]
   orca repo show --repo <selector> [--json]
   orca repo set-base-ref --repo <selector> --ref <ref> [--json]
   orca repo search-refs --repo <selector> --query <text> [--limit <n>] [--json]
--- /dev/null
+++ b/resources/app.asar.unpacked/out/cli/repo-kind-flag.js
@@ -0,0 +1,14 @@
+"use strict";
+Object.defineProperty(exports, "__esModule", { value: true });
+exports.getOptionalRepoKind = getOptionalRepoKind;
+const runtime_client_1 = require("./runtime-client");
+function getOptionalRepoKind(flags) {
+    if (!flags.has('kind')) {
+        return undefined;
+    }
+    const kind = flags.get('kind');
+    if (kind === 'git' || kind === 'folder') {
+        return kind;
+    }
+    throw new runtime_client_1.RuntimeClientError('invalid_argument', '--kind must be git or folder');
+}
--- a/resources/app.asar.unpacked/out/cli/specs/core.js
+++ b/resources/app.asar.unpacked/out/cli/specs/core.js
@@ -40,8 +40,8 @@
     {
         path: ['repo', 'add'],
         summary: 'Add a project to Orca by filesystem path',
-        usage: 'orca repo add --path <path> [--json]',
-        allowedFlags: [...args_1.GLOBAL_FLAGS, 'path']
+        usage: 'orca repo add --path <path> [--kind git|folder] [--json]',
+        allowedFlags: [...args_1.GLOBAL_FLAGS, 'path', 'kind']
     },
     {
         path: ['repo', 'show'],
