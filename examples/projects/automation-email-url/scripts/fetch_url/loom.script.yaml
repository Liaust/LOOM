kind: loom.script
id: fetch_url
name: Fetch URL
version: 0.1.0
description: Deterministic script-backed capability for the email URL automation example.

entrypoint:
  command:
    - ./run.sh

runtime:
  shell: bash

inputs: {}
outputs: {}

execution:
  timeout_seconds: 30
  network: false
  filesystem:
    mode: read_only

artifacts: []
usage_documents:
  - path: README.md
metadata:
  example: true
