# Fetch URL Script

Deterministic example script for a URL-fetch automation.

The example script does not make a network call by default; it echoes the mapped input so smoke tests stay deterministic.
