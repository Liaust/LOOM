# Research Notes

Example v0.3 project for notes-first work.

The important surface is `notes/`: it is syncable and indexable, while the whole project can still be backed up through project policy.

```sh
loom project validate .
loom project register .
loom project watch-plan .
loom project doctor research-notes --project-root .
```
